/* Mi primer programa */ 

// Declaración
let a, b;

// Asignación
a = 10;
b = a * a <= 200;

// Declaración y asignación
let c = 123;

// Re–declaración no permitida
let a;

// Re–asignación
c = 0xFF / (0b101 * (0702 - 1e-10));
